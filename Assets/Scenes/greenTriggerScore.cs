using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class greenTriggerScore : MonoBehaviour
{
   // public SpriteRenderer ColorChangingTarget;
    // Start is called before the first frame update
    private void OnCollisionEnter(Collision collision)
    {
    /*    var objectName = collision.gameObject.name;

        Debug.Log("" + objectName);
        ColorChangingTarget.color = Color.blue;
        */
    }
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        //text.GetComponent<UnityEngine.UI.Text>().text = score.ToString();

    }
}
